import { TypeProduitPipe } from './type-produit.pipe';

describe('TypeProduitPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeProduitPipe();
    expect(pipe).toBeTruthy();
  });
});
